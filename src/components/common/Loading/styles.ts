'use client'

/* Libs */
import { styled as muiStyled, CircularProgress } from "@mui/material";

export const CircularProgress_styled = muiStyled(CircularProgress)`
  margin-right: 30px;
`;
export const DivLoadding = muiStyled('div')`
  width: 100%;
  height: 500px;
  display: flex;
  justify-content: center;
  align-items: center;
`;